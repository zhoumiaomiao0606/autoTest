<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yunche.loan.dao.mapper.EmployeeRelaUserGroupDOMapper">
    <resultMap id="BaseResultMap" type="com.yunche.loan.domain.dataObj.EmployeeRelaUserGroupDO">
        <id column="employee_id" jdbcType="BIGINT" property="employeeId"/>
        <id column="user_group_id" jdbcType="BIGINT" property="userGroupId"/>
        <result column="gmt_create" jdbcType="TIMESTAMP" property="gmtCreate"/>
        <result column="gmt_modify" jdbcType="TIMESTAMP" property="gmtModify"/>
    </resultMap>

    <sql id="Base_Column_List">
    employee_id, user_group_id, gmt_create, gmt_modify
  </sql>

    <!--  count -->
    <select id="count" parameterType="com.yunche.loan.domain.QueryObj.BaseQuery"
            resultType="java.lang.Integer">
        select COUNT(*)
        from employee_rela_user_group
        WHERE  user_group_id = #{id}
    </select>

    <!-- query -->
    <select id="query" parameterType="com.yunche.loan.domain.QueryObj.BaseQuery"
            resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from employee_rela_user_group
        WHERE user_group_id = #{id}
        ORDER BY gmt_modify DESC
        limit #{startRow}, #{pageSize}
    </select>

    <!--getEmployeeIdListByUserGroupId-->
    <select id="getEmployeeIdListByUserGroupId" parameterType="java.lang.Long" resultType="java.lang.Long">
        select  employee_id
        from employee_rela_user_group
        where user_group_id = #{userGroupId,jdbcType=BIGINT}
    </select>

    <!--getUserGroupIdListByEmployeeId-->
    <select id="getUserGroupIdListByEmployeeId" parameterType="java.lang.Long" resultType="java.lang.Long">
        select user_group_id
        from employee_rela_user_group
        where employee_id = #{employeeId,jdbcType=BIGINT}
    </select>

    <!-- batchInsert -->
    <insert id="batchInsert" parameterType="java.util.List">
        insert into employee_rela_user_group (employee_id, user_group_id, gmt_create,
        gmt_modify)
        values
        <foreach collection="list" item="item" index="index" separator=",">
            (#{item.employeeId,jdbcType=BIGINT}, #{item.userGroupId,jdbcType=BIGINT},
            #{item.gmtCreate,jdbcType=TIMESTAMP},
            #{item.gmtModify,jdbcType=TIMESTAMP})
        </foreach>
    </insert>

    <select id="selectByPrimaryKey" parameterType="com.yunche.loan.domain.dataObj.EmployeeRelaUserGroupDOKey"
            resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from employee_rela_user_group
        where employee_id = #{employeeId,jdbcType=BIGINT}
        and user_group_id = #{userGroupId,jdbcType=BIGINT}
    </select>

    <delete id="deleteByPrimaryKey" parameterType="com.yunche.loan.domain.dataObj.EmployeeRelaUserGroupDOKey">
    delete from employee_rela_user_group
    where employee_id = #{employeeId,jdbcType=BIGINT}
      and user_group_id = #{userGroupId,jdbcType=BIGINT}
  </delete>

    <insert id="insert" parameterType="com.yunche.loan.domain.dataObj.EmployeeRelaUserGroupDO">
    insert into employee_rela_user_group (employee_id, user_group_id, gmt_create, 
      gmt_modify)
    values (#{employeeId,jdbcType=BIGINT}, #{userGroupId,jdbcType=BIGINT}, #{gmtCreate,jdbcType=TIMESTAMP}, 
      #{gmtModify,jdbcType=TIMESTAMP})
  </insert>

    <insert id="insertSelective" parameterType="com.yunche.loan.domain.dataObj.EmployeeRelaUserGroupDO">
        insert into employee_rela_user_group
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="employeeId != null">
                employee_id,
            </if>
            <if test="userGroupId != null">
                user_group_id,
            </if>
            <if test="gmtCreate != null">
                gmt_create,
            </if>
            <if test="gmtModify != null">
                gmt_modify,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="employeeId != null">
                #{employeeId,jdbcType=BIGINT},
            </if>
            <if test="userGroupId != null">
                #{userGroupId,jdbcType=BIGINT},
            </if>
            <if test="gmtCreate != null">
                #{gmtCreate,jdbcType=TIMESTAMP},
            </if>
            <if test="gmtModify != null">
                #{gmtModify,jdbcType=TIMESTAMP},
            </if>
        </trim>
    </insert>

    <update id="updateByPrimaryKeySelective" parameterType="com.yunche.loan.domain.dataObj.EmployeeRelaUserGroupDO">
        update employee_rela_user_group
        <set>
            <if test="gmtCreate != null">
                gmt_create = #{gmtCreate,jdbcType=TIMESTAMP},
            </if>
            <if test="gmtModify != null">
                gmt_modify = #{gmtModify,jdbcType=TIMESTAMP},
            </if>
        </set>
        where employee_id = #{employeeId,jdbcType=BIGINT}
        and user_group_id = #{userGroupId,jdbcType=BIGINT}
    </update>

    <update id="updateByPrimaryKey" parameterType="com.yunche.loan.domain.dataObj.EmployeeRelaUserGroupDO">
    update employee_rela_user_group
    set gmt_create = #{gmtCreate,jdbcType=TIMESTAMP},
      gmt_modify = #{gmtModify,jdbcType=TIMESTAMP}
    where employee_id = #{employeeId,jdbcType=BIGINT}
      and user_group_id = #{userGroupId,jdbcType=BIGINT}
  </update>

</mapper>