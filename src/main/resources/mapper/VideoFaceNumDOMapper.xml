<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.yunche.loan.mapper.VideoFaceNumDOMapper" >
  <resultMap id="BaseResultMap" type="com.yunche.loan.domain.entity.VideoFaceNumDO" >
    <id column="order_id" property="order_id" jdbcType="BIGINT" />
    <result column="face_num" property="face_num" jdbcType="INTEGER" />
  </resultMap>
  <sql id="Base_Column_List" >
    order_id, face_num
  </sql>
  <select id="selectByPrimaryKey" resultMap="BaseResultMap" parameterType="java.lang.Long" >
    select 
    <include refid="Base_Column_List" />
    from video_face_num
    where order_id = #{order_id,jdbcType=BIGINT}
  </select>
  <delete id="deleteByPrimaryKey" parameterType="java.lang.Long" >
    delete from video_face_num
    where order_id = #{order_id,jdbcType=BIGINT}
  </delete>
  <insert id="insert" parameterType="com.yunche.loan.domain.entity.VideoFaceNumDO" >
    insert into video_face_num (order_id, face_num)
    values (#{order_id,jdbcType=BIGINT}, #{face_num,jdbcType=INTEGER})
  </insert>
  <insert id="insertSelective" parameterType="com.yunche.loan.domain.entity.VideoFaceNumDO" >
    insert into video_face_num
    <trim prefix="(" suffix=")" suffixOverrides="," >
      <if test="order_id != null" >
        order_id,
      </if>
      <if test="face_num != null" >
        face_num,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides="," >
      <if test="order_id != null" >
        #{order_id,jdbcType=BIGINT},
      </if>
      <if test="face_num != null" >
        #{face_num,jdbcType=INTEGER},
      </if>
    </trim>
  </insert>
  <update id="updateByPrimaryKeySelective" parameterType="com.yunche.loan.domain.entity.VideoFaceNumDO" >
    update video_face_num
    <set >
      <if test="face_num != null" >
        face_num = #{face_num,jdbcType=INTEGER},
      </if>
    </set>
    where order_id = #{order_id,jdbcType=BIGINT}
  </update>
  <update id="updateByPrimaryKey" parameterType="com.yunche.loan.domain.entity.VideoFaceNumDO" >
    update video_face_num
    set face_num = #{face_num,jdbcType=INTEGER}
    where order_id = #{order_id,jdbcType=BIGINT}
  </update>
</mapper>