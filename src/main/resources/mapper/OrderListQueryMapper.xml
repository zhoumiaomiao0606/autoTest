<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yunche.loan.mapper.OrderListQueryMapper">

    <sql id="Base_Column_List">
        id, name, uri, menu_id, gmt_create, gmt_modify, feature, status
    </sql>

    <!-- creditApply -->
    <select id="creditApply" parameterType="com.yunche.loan.domain.query.OrderListQuery"
            resultType="com.yunche.loan.domain.vo.CreditApplyOrderListVO">
    </select>

</mapper>